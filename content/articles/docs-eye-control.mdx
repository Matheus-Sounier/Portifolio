---
title: "Blink Controller Docs"
date: 2026-05-13
---

A real-time eye blink controller built with **Python**, **MediaPipe**, **OpenCV**, and **CVZone**.

This project allows players to use the **Space** key using eye blinks captured by a webcam. Instead of pressing a physical key, the system detects when the player's eye closes and automatically triggers the **Space** key.

---

## Features

- Real-time eye tracking
- Blink detection using Eye Aspect Ratio
- Automatic Space key press and release
- Face landmark tracking using MediaPipe
- BlendShape facial analysis
- LivePlot visualization
- Webcam-based interaction

---

## Technologies

This project is built using the following libraries and frameworks:

| Technology | Documentation |
|------------|---------------|
| Python | [Python Documentation](https://docs.python.org/3/) |
| OpenCV | [OpenCV Documentation](https://docs.opencv.org/4.x/) |
| MediaPipe | [MediaPipe Documentation](https://ai.google.dev/edge/mediapipe) |
| MediaPipe Tasks Vision | [Face Landmarker Documentation](https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker/python) |
| CVZone | [CVZone GitHub Repository](https://github.com/cvzone/cvzone) |
| CVZone LivePlot | [LivePlot Documentation](https://github.com/cvzone/cvzone#liveplot) |
| NumPy | [NumPy Documentation](https://numpy.org/doc/) |
| PyAutoGUI | [PyAutoGUI Documentation](https://pyautogui.readthedocs.io/en/latest/) |

---

## How It Works

The system continuously captures frames from a webcam and processes them using **MediaPipe Face Landmarker**.

MediaPipe generates a facial model containing up to **468 facial landmarks**. For blink detection, only the landmarks around the eye region are used.

The project calculates the **Eye Aspect Ratio**, which measures the distance between eyelid landmarks.

### Eye Open

```text
Eye Open
→ Space Released
```

### Eye Closed

```text
Eye Closed
→ Space Pressed
```

The result is a simple control mechanism:

```text
Close Eye  → Jump
Open Eye   → Stop Jump
```

---

## Detection Method

The blink detection is based on the **Eye Aspect Ratio**.

EAR is calculated using specific landmarks around the eye and provides a reliable measurement of whether the eye is open or closed.

The project also uses **MediaPipe BlendShapes**, which provide facial expression scores and improve the robustness of the detection process.

---

## Requirements

Before running the project, install:

### Python 3.11

Download Python from the [official Python website](https://www.python.org/downloads/).

Verify the installation:

```bash
python --version
```

or

```bash
py --version
```

---

## Installation

Clone the repository:

```bash
git clone git@github.com:Matheus-Sounier/Eye-Control-System.git
```

Enter the project directory:

```bash
cd Eye-Control-System
```

Create a virtual environment:

```bash
python -m venv venv
```

or specify Python 3.11:

```bash
py -3.11 -m venv venv
```

Activate the virtual environment.

### Windows

```bash
venv\Scripts\activate
```

Install the dependencies:

```bash
pip install -r requirements.txt
```

---

## Running the Project

Run the main script:

```bash
python .\src\blink.py
```

If everything is configured correctly, the webcam window will open and blink detection will start automatically.

---

## Live Plot

The project includes a **LivePlot** graph provided by **CVZone** that displays blink activity in real time.

For more information, see the [CVZone LivePlot documentation](https://github.com/cvzone/cvzone#liveplot).

The graph helps visualize:

- Eye opening
- Eye closing
- EAR value changes
- Detection threshold behavior

The Y-axis represents the eye openness level measured by the system.